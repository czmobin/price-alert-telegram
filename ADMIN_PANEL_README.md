# راهنمای پنل مدیریت ربات ارزَلان

## نصب و راه‌اندازی

### مرحله 1: افزودن اولین ادمین

برای افزودن اولین ادمین، دستور زیر را اجرا کنید:

```bash
python setup_admin.py
```

این اسکریپت یک منوی تعاملی برای مدیریت ادمین‌ها فراهم می‌کند.

### مرحله 2: دریافت User ID

برای دریافت User ID تلگرام خود:

1. به ربات [@userinfobot](https://t.me/userinfobot) در تلگرام پیام دهید
2. User ID خود را کپی کنید
3. در اسکریپت `setup_admin.py` وارد کنید

### مرحله 3: دسترسی به پنل ادمین

پس از افزودن خود به عنوان ادمین:

1. به ربات خود پیام دهید
2. دستور `/admin` را ارسال کنید
3. پنل مدیریت برای شما نمایش داده می‌شود

## امکانات پنل ادمین

### 📊 آمار کلی

نمایش آمار کلی ربات شامل:
- تعداد کل کاربران
- تعداد کاربران فعال
- کاربران جدید (7 و 30 روز اخیر)
- تعداد کل پیام‌ها
- تعداد کاربران با اعلان فعال

### 👥 آمار کاربران

آمار تفصیلی کاربران:
- تعداد کاربران فعال و غیرفعال
- کاربران جدید در بازه‌های مختلف (24 ساعت، 7 روز، 30 روز)
- نرخ رشد هفتگی و ماهانه

### 📨 آمار پیام‌ها

تفکیک پیام‌ها بر اساس نوع:
- درخواست قیمت
- اعلان‌های زمان‌بندی شده
- به‌روزرسانی‌ها
- شروع (start)

### 🔥 محبوب‌ترین ارزها

نمایش 10 ارز پرطرفدار که کاربران بیشتر انتخاب کرده‌اند

### 📈 فعالیت کاربران

آمار کاربران فعال در بازه‌های زمانی مختلف:
- 24 ساعت اخیر
- 7 روز اخیر
- 30 روز اخیر

### 👤 کاربران اخیر

نمایش 10 کاربر آخری که به ربات پیوسته‌اند

## مدیریت ادمین‌ها

### افزودن ادمین جدید

```bash
python setup_admin.py
```

سپس گزینه 1 را انتخاب کنید و User ID ادمین جدید را وارد کنید.

### حذف ادمین

```bash
python setup_admin.py
```

سپس گزینه 3 را انتخاب کنید و User ID ادمین مورد نظر برای حذف را وارد کنید.

### نمایش لیست ادمین‌ها

```bash
python setup_admin.py
```

سپس گزینه 2 را انتخاب کنید.

## نکات امنیتی

⚠️ **مهم:**
- User ID ادمین‌ها را به صورت محرمانه نگه دارید
- فقط افراد مورد اعتماد را به عنوان ادمین اضافه کنید
- پشتیبان منظم از دیتابیس (`bot_database.db`) تهیه کنید

## ساختار دیتابیس

پنل ادمین از یک جدول جدید در دیتابیس استفاده می‌کند:

```sql
CREATE TABLE admins (
    user_id INTEGER PRIMARY KEY,
    added_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    added_by INTEGER,
    FOREIGN KEY (user_id) REFERENCES users(user_id)
)
```

## استفاده در تلگرام

پس از راه‌اندازی، ادمین‌ها می‌توانند با ارسال دستور `/admin` به پنل مدیریت دسترسی داشته باشند:

```
/admin
```

سپس از دکمه‌های inline برای نمایش آمارهای مختلف استفاده کنید.

## عیب‌یابی

### خطا: "شما دسترسی به پنل ادمین ندارید"

این خطا به این معنی است که User ID شما در جدول ادمین‌ها ثبت نشده است. برای رفع این مشکل:

1. `setup_admin.py` را اجرا کنید
2. User ID خود را اضافه کنید
3. دوباره `/admin` را امتحان کنید

### خطا در دریافت آمار

اگر آمارها به درستی نمایش داده نمی‌شوند:

1. مطمئن شوید دیتابیس به درستی ایجاد شده است
2. ربات را ریستارت کنید
3. لاگ‌های ربات را بررسی کنید

## به‌روزرسانی‌های آینده

امکانات برنامه‌ریزی شده برای نسخه‌های آینده:

- [ ] ارسال پیام گروهی به کاربران
- [ ] export آمار به فرمت CSV/Excel
- [ ] نمودارهای گرافیکی آمار
- [ ] مدیریت کاربران (فعال/غیرفعال کردن)
- [ ] لاگ فعالیت‌های ادمین‌ها
