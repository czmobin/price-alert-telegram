# 📊 ربات تلگرام اطلاع‌رسانی قیمت سرمایه‌گذاری

یک ربات تلگرامی پیشرفته برای دریافت قیمت لحظه‌ای ارزهای دیجیتال، طلا، نقره و دلار با قابلیت زمان‌بندی و شخصی‌سازی کامل.

## ✨ ویژگی‌ها

- 🪙 **دریافت قیمت ارزهای دیجیتال**: نمایش قیمت لحظه‌ای بیش از 15 ارز دیجیتال محبوب
- 🥇 **قیمت طلا و نقره**: نمایش قیمت جهانی طلا و نقره (به دلار)
- 💵 **قیمت دلار**: نمایش قیمت دلار آمریکا به تومان
- 📈 **تغییرات هفتگی**: نمایش درصد تغییرات 24 ساعته و 7 روزه
- ⏰ **زمان‌بندی هوشمند**: دریافت خودکار گزارش در ساعت دلخواه
- 🎨 **شخصی‌سازی کامل**: انتخاب ارزها و دارایی‌های مورد نظر
- 🔄 **به‌روزرسانی آنی**: دکمه به‌روزرسانی سریع قیمت‌ها
- 💾 **پایگاه داده**: ذخیره تنظیمات و تاریخچه کاربران
- 🔔 **اعلان‌های خودکار**: ارسال گزارش روزانه در زمان تعیین شده

## 🚀 نصب و راه‌اندازی

### پیش‌نیازها

- Python 3.8 یا بالاتر
- pip (مدیر بسته‌های پایتون)
- اکانت تلگرام

### 1. دریافت توکن ربات تلگرام

1. در تلگرام به [@BotFather](https://t.me/BotFather) پیام دهید
2. دستور `/newbot` را ارسال کنید
3. نام و username ربات خود را وارد کنید
4. توکن دریافتی را کپی کنید

### 2. نصب پروژه

```bash
# کلون کردن ریپازیتوری
git clone https://github.com/your-username/price-alert-telegram.git
cd price-alert-telegram

# نصب وابستگی‌ها
pip install -r requirements.txt

# یا با استفاده از virtual environment (توصیه می‌شود)
python -m venv venv
source venv/bin/activate  # در لینوکس/Mac
# یا
venv\Scripts\activate  # در ویندوز

pip install -r requirements.txt
```

### 3. تنظیمات

1. فایل `.env` را ایجاد کنید:
```bash
cp .env.example .env
```

2. فایل `.env` را ویرایش کنید و توکن ربات خود را وارد کنید:
```env
TELEGRAM_BOT_TOKEN=YOUR_BOT_TOKEN_HERE
```

### 4. اجرای ربات

```bash
python bot.py
```

## 📱 نحوه استفاده

### دستورات اصلی

- `/start` - شروع کار با ربات و ثبت شماره تلفن
- `/price` - دریافت قیمت‌های فعلی
- `/settings` - مشاهده و تغییر تنظیمات
- `/schedule` - تنظیم زمان ارسال خودکار
- `/cryptos` - انتخاب ارزهای دیجیتال
- `/help` - نمایش راهنمای کامل

### راهنمای گام به گام

1. **ثبت نام**:
   - دستور `/start` را ارسال کنید
   - شماره تلفن خود را با کلیک روی دکمه به اشتراک بگذارید

2. **دریافت قیمت‌ها**:
   - دستور `/price` را ارسال کنید
   - برای به‌روزرسانی روی دکمه 🔄 کلیک کنید

3. **شخصی‌سازی**:
   - از دستور `/settings` برای دسترسی به تنظیمات استفاده کنید
   - ارزهای مورد نظر خود را از لیست انتخاب کنید
   - دارایی‌های دیگر (طلا، نقره، دلار) را فعال/غیرفعال کنید

4. **تنظیم زمان‌بندی**:
   - از منوی تنظیمات، گزینه "تنظیم زمان‌بندی" را انتخاب کنید
   - ساعت دلخواه خود را به فرمت `HH:MM` وارد کنید (مثال: `09:00`)
   - ربات هر روز در این ساعت گزارش را برای شما ارسال می‌کند

## 🏗️ معماری پروژه

```
price-alert-telegram/
├── bot.py              # فایل اصلی ربات و logic اصلی
├── database.py         # مدیریت پایگاه داده SQLite
├── price_fetcher.py    # دریافت قیمت‌ها از APIها
├── config.py           # تنظیمات و پیکربندی
├── requirements.txt    # وابستگی‌های پایتون
├── .env               # متغیرهای محیطی (توکن ربات)
├── .env.example       # نمونه فایل محیطی
├── .gitignore         # فایل‌های ignore شده
└── README.md          # مستندات
```

### ماژول‌ها

#### bot.py
- مدیریت دستورات تلگرام
- Handler های مختلف برای callback ها
- زمان‌بندی و ارسال خودکار پیام‌ها
- رابط کاربری (دکمه‌ها و منوها)

#### database.py
- مدیریت پایگاه داده SQLite
- جداول: users, user_settings, message_history
- CRUD operations برای کاربران و تنظیمات

#### price_fetcher.py
- دریافت قیمت ارزهای دیجیتال از CoinGecko API
- دریافت قیمت طلا و نقره
- دریافت قیمت دلار از APIهای ایرانی
- فرمت کردن و آماده‌سازی پیام‌ها

#### config.py
- تنظیمات عمومی ربات
- لیست ارزهای پشتیبانی شده
- آدرس APIها
- تنظیمات زمانی

## 🔌 APIهای مورد استفاده

1. **CoinGecko API**: برای دریافت قیمت ارزهای دیجیتال (رایگان)
2. **Gold API**: برای دریافت قیمت طلا
3. **AccessBan/Bonbast API**: برای دریافت قیمت دلار

## 🎯 ارزهای پشتیبانی شده

- Bitcoin (BTC)
- Ethereum (ETH)
- Binance Coin (BNB)
- Cardano (ADA)
- Ripple (XRP)
- Solana (SOL)
- Polkadot (DOT)
- Dogecoin (DOGE)
- Tether (USDT)
- USD Coin (USDC)
- Litecoin (LTC)
- Chainlink (LINK)
- Avalanche (AVAX)
- Tron (TRX)
- Stellar (XLM)

## 🔧 توسعه و سفارشی‌سازی

### اضافه کردن ارز جدید

در فایل `config.py`:

```python
CRYPTO_SYMBOLS = {
    # ارزهای موجود...
    'new-coin-id': 'SYMBOL',  # ID از CoinGecko
}
```

### تغییر منبع قیمت

در فایل `price_fetcher.py` می‌توانید متدهای دریافت قیمت را تغییر دهید:

```python
def get_custom_price(self):
    # کد دریافت قیمت از منبع دلخواه
    pass
```

### اضافه کردن دستور جدید

در فایل `bot.py`:

```python
async def new_command(self, update: Update, context: ContextTypes.DEFAULT_TYPE):
    # منطق دستور جدید
    await update.message.reply_text("پاسخ")

# در تابع run():
self.application.add_handler(CommandHandler('new', self.new_command))
```

## 📊 پایگاه داده

### ساختار جداول

**users**:
- user_id (PRIMARY KEY)
- username, first_name, last_name
- phone_number
- created_at, last_interaction

**user_settings**:
- user_id (FOREIGN KEY)
- notification_enabled, notification_time
- selected_cryptos (JSON)
- include_gold, include_silver, include_usd
- timezone

**message_history**:
- id (PRIMARY KEY)
- user_id (FOREIGN KEY)
- message_type
- sent_at

## 🛠️ عیب‌یابی

### ربات پاسخ نمی‌دهد
- توکن ربات را در فایل `.env` بررسی کنید
- اتصال اینترنت را چک کنید
- لاگ‌ها را مشاهده کنید

### قیمت‌ها نمایش داده نمی‌شوند
- اتصال به APIها را بررسی کنید
- ممکن است APIها موقتاً در دسترس نباشند
- از VPN استفاده کنید (برای برخی APIها)

### زمان‌بندی کار نمی‌کند
- timezone را در `config.py` بررسی کنید
- scheduler را restart کنید
- لاگ‌های scheduler را بررسی کنید

## 🤝 مشارکت

برای مشارکت در توسعه این پروژه:

1. Fork کنید
2. یک branch جدید بسازید (`git checkout -b feature/amazing-feature`)
3. تغییرات خود را commit کنید (`git commit -m 'Add amazing feature'`)
4. Push کنید (`git push origin feature/amazing-feature`)
5. یک Pull Request باز کنید

## 📝 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است.

## 📧 تماس و پشتیبانی

برای سوالات و مشکلات، یک Issue در GitHub باز کنید.

## 🌟 ویژگی‌های آینده

- [ ] پشتیبانی از چند زبان
- [ ] نمودار قیمت‌ها
- [ ] هشدارهای قیمتی (Price Alerts)
- [ ] مقایسه قیمت‌ها در بازه‌های زمانی
- [ ] صرافی‌های ایرانی
- [ ] پشتیبانی از کانال‌های تلگرام
- [ ] رابط وب (Web Dashboard)

## 🙏 تشکر

از APIهای زیر استفاده شده است:
- [CoinGecko](https://www.coingecko.com/)
- [python-telegram-bot](https://python-telegram-bot.org/)

---

**ساخته شده با ❤️ برای جامعه سرمایه‌گذاران ایرانی**
